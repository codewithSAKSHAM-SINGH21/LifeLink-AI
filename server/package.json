{
  "name": "lifelink-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon --signal SIGTERM --watch index.js --watch routes --watch models --watch middleware --ext js,mjs,json index.js",
    "start": "node index.js",
    "build": "node --check index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
